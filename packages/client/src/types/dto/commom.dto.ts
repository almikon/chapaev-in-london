export type Id = {
  id: number;
};

export type Avatar = {
  avatar: string | null;
};

export type Role = {
  role: string;
};

export type Limit = {
  limit: number;
};

export type Cursor = {
  cursor: number;
};